services:
  perses:
    image: persesdev/perses:latest
    container_name: perses
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - perses_data:/perses/data
    command:
      - --config=/perses/data/config.yaml

volumes:
  perses_data:
